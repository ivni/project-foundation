interface:
  display_name: "Codex Review Loop"
  short_description: "Run an independent Codex review-fix loop"
  default_prompt: "Use $run-codex-review-loop to review the uncommitted changes for this task, fix accepted findings, and re-review until clean or blocked."
policy:
  allow_implicit_invocation: true
