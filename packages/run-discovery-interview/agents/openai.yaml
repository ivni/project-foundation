interface:
  display_name: "Discovery Interview"
  short_description: "Clarify product value, behavior, and UX"
  default_prompt: "Use $run-discovery-interview to clarify this feature's value, functionality, UX, and business rules before implementation."

policy:
  allow_implicit_invocation: true
