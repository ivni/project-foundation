interface:
  display_name: "Qwen Review Loop"
  short_description: "Run an independent Qwen review-fix loop"
  default_prompt: "Use $run-qwen-review-loop to review the uncommitted changes for this task, fix accepted findings, and re-review until clean or blocked."
policy:
  allow_implicit_invocation: true
